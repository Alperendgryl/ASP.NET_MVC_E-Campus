
@{
    ViewBag.Title = "ListOfProjects";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>@ViewData["Title"]</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="~/Content/StyleSheet.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
</head>
<body class="bg-white">

    <div class="row" style="padding-right:20px; padding-left:20px;">
        <div class="col bg-white">
            <div class="card">
                <div class="card-header bg-blue text-white">
                    <h4 class="text-light" style="height: 8px; font-family: Arial; font-size:18px">List Of Projects</h4>
                </div>
                <li class="list-group-item border px-3 pb-3 pt-1">
                    <table class="col text-dark" border="1" cellspacing="2" cellpadding="20">
                        <tr>
                            <th>Project Name</th>
                            <th>Budget</th>
                            <th>Start Date</th>
                            <th>End Date</th>
                            <th>Subject</th>
                            <th>Lead SSN</th>
                            <th>Contr D.Name</th>

                        </tr>
                        @foreach (var ListOfProjects in Model)
                        {
                            <tr>
                                <td>@ListOfProjects.prName</td>
                                <td>@ListOfProjects.budget</td>
                                <td>@ListOfProjects.startDate</td>
                                <td>@ListOfProjects.endDate</td>
                                <td>@ListOfProjects.subject</td>
                                <td>@ListOfProjects.leadSsn</td>
                                <td>@ListOfProjects.contrDName</td>
                            </tr>
                        }
                    </table>
                </li>
            </div>
        </div>
    </div>
</body>
</html>

